[project]
name = "klava-tools-mcp"
version = "0.1.0"
description = "MCP server bridging the Klava task queue + dashboard primitives."
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0"]

[project.scripts]
klava-tools-mcp = "klava_tools_mcp.main:main"
